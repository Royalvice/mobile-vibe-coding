import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mvc.app',
  appName: 'mobile-vibe-coding',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
